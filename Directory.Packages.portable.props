<!-- Portable build package overrides.
     WASDK stays at upstream 2.3.1 — the WMC9999 XamlCompiler.exe bug is
     fixed via UseXamlCompilerExecutable=false in Directory.Build.portable.props. -->
<Project>
</Project>
